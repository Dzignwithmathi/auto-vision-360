Drop final renders/photography here using the filenames referenced throughout
`components/sections/*.tsx` (e.g. `hero-auto-render.png`, `feature-panorama.png`,
`visibility-before.png` / `visibility-after.png`). See the main README's
"Replacing placeholder images" section for the exact swap steps.
